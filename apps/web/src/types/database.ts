/**
 * Re-export dos tipos do banco de dados gerados pelo Supabase.
 * Fonte da verdade: packages/shared/database.types.ts
 *
 * Para regenerar: pnpm supabase:gen-types
 */
export type { Database, Json } from '@versix/shared';
